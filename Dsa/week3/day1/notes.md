## sorting

# Bubble sort
if two neighbours are in the wrong order => swap them
har pass ke baad ek larget element end me aa jeaga from the unsorted part

# insertion sort
insert the things on their right places
    - store the i in the temporary variable
    - create a j = i-1;
    - check if (j>temp)
    - then => a[[j+1]] = a[[j]]
    - else after loop stops put it in a[[j+1]] = temp



